[package]
name = "slate-sql"
version = "0.1.0"
edition = "2024"

[dependencies]
bson = { workspace = true }
regex = "1"

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "sql"
harness = false
