[package]
name = "slate-vm"
version = "0.1.0"
edition = "2024"

[dependencies]
bson = { workspace = true }
mlua = { version = "0.11", features = ["lua54", "vendored", "send"] }

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "vm"
harness = false
