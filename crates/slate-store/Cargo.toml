[package]
name = "slate-store"
version = "0.1.0"
edition = "2024"

[features]
default = ["rocksdb"]
rocksdb = ["dep:rocksdb"]

[dependencies]
rocksdb = { version = "0.22", optional = true }
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
tempfile = "3"
