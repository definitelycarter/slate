services:
  db:
    build:
      context: ..
      dockerfile: dev/Dockerfile
      target: server
    ports:
      - "9600:9600"
    environment:
      SLATE_ADDR: 0.0.0.0:9600

  init:
    build:
      context: ..
      dockerfile: dev/Dockerfile
      target: server-init
    depends_on:
      - db
    environment:
      SLATE_SERVER_ADDR: db:9600
      SLATE_INIT_CONFIG: /etc/slate/collections.json
    volumes:
      - ./collections.json:/etc/slate/collections.json:ro

  lists:
    build:
      context: ..
      dockerfile: dev/Dockerfile
      target: lists
    ports:
      - "8080:8080"
    depends_on:
      init:
        condition: service_completed_successfully
    environment:
      SLATE_SERVER_ADDR: db:9600
      SLATE_LIST_CONFIG: /etc/slate/list.json
      SLATE_POOL_SIZE: "4"
    volumes:
      - ./list.json:/etc/slate/list.json:ro
